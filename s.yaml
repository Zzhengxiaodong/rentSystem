edition: 3.0.0
name: rentSystemt
access: default

resources:
  fcDemo:
    component: fc3
    props:
      region: cn-shenzhen
      code: '/code/lib:/usr/local/lib:/opt/lib:/opt/php8.1/lib:/opt/php8.0/lib:/opt/php7.2/lib'
      cpu: 0.05
      description: test-rentSystemt-aaa
      diskSize: 512
      functionName: rentSystemt-hhh
      handler: string
      memorySize: 128
      runtime: custom
      disSize: 512
      triggers:
        triggerName: httpTrigger
        triggerType: http
        triggerConfig:
          authType: anonymous
          methods:
            - GET
            - POST
            - PUT
            - DELETE
            - HEAD
            - PATCH
